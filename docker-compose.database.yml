# TinyInsta — infrastructure & datastores.
# Profiles: infra | mongo | minio | neo4j | search | cassandra | tools | observability
#
# Included by the root docker-compose.yml. Holds everything that is NOT one of
# our application services: the API gateway, auth, message bus, datastores and
# the observability stack — plus the named volumes that persist their data.

services:
  # ===========================================================================
  # INFRA (profile: infra)
  # ===========================================================================
  traefik:
    image: traefik:v3.1
    profiles: ["infra"]
    command: --configFile=/etc/traefik/traefik.yml
    ports:
      - "80:80"       # API Gateway (frontend → /api/... and /ws)
      - "8090:8090"   # Traefik dashboard
    volumes:
      - ./infra/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./infra/traefik/dynamic.yml:/etc/traefik/dynamic.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro

  postgres:
    image: postgres:16
    profiles: ["infra"]
    environment:
      POSTGRES_USER: "${POSTGRES_USER:-tinyinsta}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-tinyinsta}"
      POSTGRES_DB: "${POSTGRES_USER:-tinyinsta}"
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tinyinsta}"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7
    profiles: ["infra"]
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  redpanda:
    image: redpandadata/redpanda:v24.2.7
    profiles: ["infra"]
    command:
      - redpanda
      - start
      - --smp=1
      - --overprovisioned
      - --kafka-addr=PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr=PLAINTEXT://redpanda:9092
    ports:
      - "9092:9092"
    volumes:
      - redpanda-data:/var/lib/redpanda/data
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -q 'Healthy:.*true'"]
      interval: 5s
      timeout: 5s
      retries: 15

  # Pre-create the event topics so consumers subscribe to topics that already
  # exist. Without this, a topic is created lazily on its first publish and a
  # worker that subscribed earlier only notices it on the next metadata refresh
  # (~5 min) — e.g. post.deleted would be ignored until then. Idempotent.
  redpanda-init:
    image: redpandadata/redpanda:v24.2.7
    profiles: ["infra"]
    depends_on:
      redpanda:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        rpk topic create -X brokers=redpanda:9092
        user.created user.followed user.unfollowed
        user.blocked user.unblocked user.mentioned
        user.close_friend_added user.close_friend_removed
        post.created post.commented post.comment_edited post.comment_deleted
        post.deleted post.liked post.unliked
        post.saved post.unsaved post.reposted post.unreposted
        media.uploaded media.processed
        story.created story.viewed
        message.sent
        dlq.post-svc dlq.usertimeline-svc dlq.search-svc
        dlq.hometimeline-svc dlq.realtime-svc dlq.stories-svc
        dlq.media-worker dlq.ranking-svc dlq.messaging-svc || true
    restart: "no"

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    profiles: ["infra"]
    command: start-dev --import-realm
    environment:
      KEYCLOAK_ADMIN: "${KEYCLOAK_ADMIN:-admin}"
      KEYCLOAK_ADMIN_PASSWORD: "${KEYCLOAK_ADMIN_PASSWORD:-admin}"
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: "${POSTGRES_USER:-tinyinsta}"
      KC_DB_PASSWORD: "${POSTGRES_PASSWORD:-tinyinsta}"
      KC_HEALTH_ENABLED: "true"
    ports:
      - "8080:8080"
    volumes:
      - ./infra/keycloak:/opt/keycloak/data/import:ro
    depends_on:
      postgres:
        condition: service_healthy

  # ===========================================================================
  # DATASTORES (introduced incrementally via profiles)
  # ===========================================================================
  mongo:
    image: mongo:7
    profiles: ["mongo"]
    # Single-node replica set; initialised once by the mongo-init one-shot below.
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 10

  mongo-init:
    image: mongo:7
    profiles: ["mongo"]
    depends_on:
      mongo:
        condition: service_healthy
    restart: "no"
    # Initialise the replica set (idempotent: re-runs swallow "already initialized").
    entrypoint:
      - bash
      - -c
      - |
        mongosh --host mongo:27017 --quiet --eval '
          try { rs.initiate({_id:"rs0",members:[{_id:0,host:"mongo:27017"}]}); }
          catch (e) { if (!/already initialized/.test(e.message)) throw e; }
        '

  minio:
    image: minio/minio:latest
    profiles: ["minio"]
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: "${S3_ACCESS_KEY:-minioadmin}"
      MINIO_ROOT_PASSWORD: "${S3_SECRET_KEY:-minioadmin}"
    ports:
      - "9000:9000"   # API S3
      - "9001:9001"   # console
    volumes:
      - minio-data:/data

  # Caching CDN in front of MinIO for media reads. Routed by Traefik on /cdn;
  # media-svc/worker hand out read URLs through it (S3_CDN_URL). Uploads bypass it.
  nginx-cdn:
    image: nginx:1.27-alpine
    profiles: ["minio"]
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.cdn.rule=PathPrefix(`/cdn`)"
      - "traefik.http.routers.cdn.middlewares=cdnstrip@file,ratelimit@file"
      - "traefik.http.services.cdn.loadbalancer.server.port=80"
    depends_on:
      - minio

  neo4j:
    image: neo4j:5
    profiles: ["neo4j"]
    environment:
      NEO4J_AUTH: "${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-tinyinsta}"
      # JVM heap + page cache caps.
      NEO4J_server_memory_heap_initial__size: "256m"
      NEO4J_server_memory_heap_max__size: "512m"
      NEO4J_server_memory_pagecache_size: "256m"
    mem_limit: 1g
    ports:
      - "7474:7474"   # console
      - "7687:7687"   # bolt
    volumes:
      - neo4j-data:/data

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.0
    profiles: ["search"]
    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"
    mem_limit: 1200m
    ports:
      - "9200:9200"
    volumes:
      - es-data:/usr/share/elasticsearch/data
    # Gate search-svc/worker on ES being up (see their depends_on).
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

  cassandra:
    image: cassandra:5
    profiles: ["cassandra"]
    environment:
      CASSANDRA_CLUSTER_NAME: tinyinsta
      # Single-node dev heap; pin both heap and direct memory.
      MAX_HEAP_SIZE: "512M"
      HEAP_NEWSIZE: "100M"
      JVM_EXTRA_OPTS: "-XX:MaxDirectMemorySize=512M"
    mem_limit: 1400m
    ports:
      - "9042:9042"
    volumes:
      - cassandra-data:/var/lib/cassandra
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'describe keyspaces' || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 20

  redpanda-console:            # tools profile (optional)
    image: redpandadata/console:v2.7.2
    profiles: ["tools"]
    environment:
      KAFKA_BROKERS: redpanda:9092
    ports:
      - "8085:8080"
    depends_on:
      - redpanda

  # ===========================================================================
  # OBSERVABILITY (profile: observability)
  #   Prometheus + Grafana for metrics; Filebeat + Kibana for the JSON logs.
  #   Kibana reuses the `search` Elasticsearch — start it alongside:
  #     docker compose --profile search --profile observability up -d
  # ===========================================================================
  prometheus:
    image: prom/prometheus:v2.54.1
    profiles: ["observability"]
    command:
      - --config.file=/etc/prometheus/prometheus.yml
    volumes:
      - ./infra/observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:11.2.0
    profiles: ["observability"]
    environment:
      GF_SECURITY_ADMIN_USER: "${GRAFANA_USER:-admin}"
      GF_SECURITY_ADMIN_PASSWORD: "${GRAFANA_PASSWORD:-admin}"
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - ./infra/observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./infra/observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana
    ports:
      - "3001:3000"
    depends_on:
      - prometheus

  filebeat:
    image: docker.elastic.co/beats/filebeat:8.15.0
    profiles: ["observability"]
    user: root
    command: ["filebeat", "-e", "-strict.perms=false"]
    volumes:
      - ./infra/observability/filebeat/filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro

  kibana:
    image: docker.elastic.co/kibana/kibana:8.15.0
    profiles: ["observability"]
    environment:
      ELASTICSEARCH_HOSTS: "http://elasticsearch:9200"
    ports:
      - "5601:5601"

volumes:
  postgres-data:
  redis-data:
  redpanda-data:
  mongo-data:
  minio-data:
  neo4j-data:
  es-data:
  cassandra-data:
  grafana-data:
